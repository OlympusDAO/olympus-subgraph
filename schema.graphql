type Ohmie @entity {
  "address of user account"
  id: ID!
  daiBondDeposits: [DaiBondDeposit!]! @derivedFrom(field: "ohmie")
  daiBondWithdrawals: [DaiBondRedeem!]! @derivedFrom(field: "ohmie")
  daiBondTotalDeposit : BigInt
}

type DaiBondDeposit @entity {
  id: ID!
  transaction: Transaction!
  ohmie: Ohmie!
  amount: BigInt!
  maxPremium: BigInt!
}

type DaiBondRedeem @entity {
  id: ID!
  transaction: Transaction!
  ohmie: Ohmie!
}

type Transaction @entity {
  "transaction hash"
  id: ID!
  timestamp: BigInt!
  blockNumber: BigInt!
  blockHash: Bytes!
  from: Bytes!
  to: Bytes
  value: BigInt!
  gasUsed: BigInt!
  gasPrice: BigInt!
}