type Treasury @entity {
  "address of user account"
  id: ID!
  bondDeposits: [Deposit!]! @derivedFrom(field: "treasury")
  bondWithdrawals: [Redeemtion!]! @derivedFrom(field: "treasury")
  daiBondTotalDeposit : BigDecimal
  ohmDaiSlpTotalDeposit : BigDecimal
  daiBalance: BigDecimal
  ohmDaiSlpBalance: BigDecimal
}

type Ohmie @entity {
  "address of user account"
  id: ID!
  bondDeposits: [Deposit!]! @derivedFrom(field: "ohmie")
  bondWithdrawals: [Redeemtion!]! @derivedFrom(field: "ohmie")
  sentOhmTransactions: [OhmTransaction!]! @derivedFrom(field: "ohmieFrom")
  receivedOhmTransactions: [OhmTransaction!]! @derivedFrom(field: "ohmieTo")
  sentSohmTransactions: [SohmTransaction!]! @derivedFrom(field: "ohmieFrom")
  receivedSohmTransactions: [SohmTransaction!]! @derivedFrom(field: "ohmieTo")
  daiBondTotalDeposit: BigDecimal
  ohmDaiSlpTotalDeposit: BigDecimal
  ohmBalance: BigDecimal
  sohmBalance: BigDecimal
}

type Deposit @entity {
  id: ID!
  transaction: Transaction!
  ohmie: Ohmie!
  amount: BigDecimal!
  maxPremium: BigDecimal!
  token: Token!
  treasury: Treasury!
}

type Redeemtion @entity {
  id: ID!
  transaction: Transaction!
  ohmie: Ohmie!
  token: Token!
  treasury: Treasury!
}

type Transaction @entity {
  "transaction hash"
  id: ID!
  timestamp: BigInt!
  blockNumber: BigInt!
  blockHash: Bytes!
  from: Bytes!
  to: Bytes
  value: BigInt!
  gasUsed: BigInt!
  gasPrice: BigInt!
}

type OhmTransaction @entity {
  id: ID!
  transaction: Transaction!
  ohmieTo: Ohmie!
  ohmieFrom: Ohmie!
  amount: BigDecimal!
}

type SohmTransaction @entity {
  id: ID!
  transaction: Transaction!
  ohmieTo: Ohmie!
  ohmieFrom: Ohmie!
  amount: BigDecimal!
}

type Token @entity {
  id: ID!
  name: String!
  address: Bytes!
}